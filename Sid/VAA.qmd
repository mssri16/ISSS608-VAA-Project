---
title: "VAAProject"
author: "Siddharth Singh"
date: 21 March 2023
date-modified: "`r Sys.Date()`"
execute: 
  echo: true
  eval: true
  warning: false
format: html
editor: visual
---

```{r}
#movies <- read_rds("data/moviestidied.rds")
```

```{r}
#genres <- movies['genres']
#genres <- unlist(genres)
#genres <- unique(genres)
#genres <- sort(genres)
```

```{r}
#productionCompanies <- movies['production_companies']
#productionCompanies <- unlist(productionCompanies)
#productionCompanies <- unique(productionCompanies)
#productionCompanies <- sort(productionCompanies)
```

```{r}
#credits <- movies['credits']
#credits <- unlist(credits)
#credits <- unique(credits)
#credits <- sort(credits)
```

```{r}
#keywords <- movies['keywords']
#keywords <- unlist(keywords)
#keywords <- unique(keywords)
#keywords <- sort(keywords)
```

```{r}
#print(genres)
```

```{r}
#print(productionCompanies)
```

```{r}
#print(credits)
```

```{r}
#print(keywords)
```
